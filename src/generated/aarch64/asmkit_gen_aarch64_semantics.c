/*
 * Generated by asmkit-tblgen from LLVM llvmorg-22.1.7 target records.
 * Do not edit by hand.
 * SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
 */
#include <stdint.h>
#include "core/asmkit_internal.h"
#include "asmkit_gen_aarch64_defs.h"

#if ASMKIT_ENABLE_TEXT
#define ASMKIT_GENERATED_OPTIONAL_TEXT_LITERAL(literal) literal
#else
#define ASMKIT_GENERATED_OPTIONAL_TEXT_LITERAL(literal) ((const char*)0)
#endif


#if defined(ASMKIT_GEN_AARCH64_SEMANTICS_REQUIRE_NONEMPTY) && ASMKIT_GEN_AARCH64_SEMANTICS_REQUIRE_NONEMPTY
typedef char asmkit_gen_aarch64_requires_nonempty[(ASMKIT_A64_PAC + 1u) > 0u ? 1 : -1];
#endif

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_feature_infos.inc"

const asmkit_feature_info_t* asmkit_gen_aarch64_feature_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_feature_infos)) {
        return 0;
    }
    return &asmkit_aarch64_feature_infos[index];
}

const asmkit_feature_info_t* asmkit_gen_aarch64_feature_info(uint32_t id)
{
    return asmkit_gen_aarch64_feature_info_by_index(id);
}

uint32_t asmkit_gen_aarch64_feature_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_feature_infos);
}

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_predicate_infos.inc"

const asmkit_predicate_info_t* asmkit_gen_aarch64_predicate_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_predicate_infos)) {
        return 0;
    }
    return &asmkit_aarch64_predicate_infos[index];
}

const asmkit_predicate_info_t* asmkit_gen_aarch64_predicate_info(uint32_t id)
{
    return asmkit_gen_aarch64_predicate_info_by_index(id);
}

uint32_t asmkit_gen_aarch64_predicate_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_predicate_infos);
}

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_cpu_feature_ids.inc"

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_cpu_infos.inc"

const asmkit_cpu_info_t* asmkit_gen_aarch64_cpu_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_cpu_infos)) {
        return 0;
    }
    return &asmkit_aarch64_cpu_infos[index];
}

const asmkit_cpu_info_t* asmkit_gen_aarch64_cpu_info(uint32_t id)
{
    return asmkit_gen_aarch64_cpu_info_by_index(id);
}

const asmkit_feature_info_t* asmkit_gen_aarch64_cpu_feature_info(uint32_t id, uint32_t index)
{
    const asmkit_cpu_info_t* cpu;
    uint32_t feature_id;

    cpu = asmkit_gen_aarch64_cpu_info(id);
    if (cpu == 0 || index >= cpu->feature_count) {
        return 0;
    }
    if (cpu->feature_index + index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_cpu_feature_ids)) {
        return 0;
    }
    feature_id = asmkit_aarch64_cpu_feature_ids[cpu->feature_index + index];
    return asmkit_gen_aarch64_feature_info(feature_id);
}

uint32_t asmkit_gen_aarch64_cpu_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_cpu_infos);
}

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_register_infos.inc"

const asmkit_register_info_t* asmkit_gen_aarch64_register_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_register_infos)) {
        return 0;
    }
    return &asmkit_aarch64_register_infos[index];
}

const asmkit_register_info_t* asmkit_gen_aarch64_register_info(uint32_t id)
{
    size_t i;
    for (i = 0; i < ASMKIT_ARRAY_COUNT(asmkit_aarch64_register_infos); ++i) {
        if (asmkit_aarch64_register_infos[i].id == id) {
            return &asmkit_aarch64_register_infos[i];
        }
    }
    return 0;
}

uint32_t asmkit_gen_aarch64_register_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_register_infos);
}

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_operand_type_infos.inc"

const asmkit_operand_type_info_t* asmkit_gen_aarch64_operand_type_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_operand_type_infos)) {
        return 0;
    }
    return &asmkit_aarch64_operand_type_infos[index];
}

const asmkit_operand_type_info_t* asmkit_gen_aarch64_operand_type_info(uint32_t id)
{
    size_t i;
    for (i = 0; i < ASMKIT_ARRAY_COUNT(asmkit_aarch64_operand_type_infos); ++i) {
        if (asmkit_aarch64_operand_type_infos[i].id == id) {
            return &asmkit_aarch64_operand_type_infos[i];
        }
    }
    return 0;
}

uint32_t asmkit_gen_aarch64_operand_type_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_operand_type_infos);
}

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_operand_infos.inc"

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_register_effect_infos.inc"

#include "asmkit_gen_aarch64_semantics_asmkit_aarch64_instruction_infos.inc"

const asmkit_instruction_info_t* asmkit_gen_aarch64_instruction_info_by_index(uint32_t index)
{
    if (index >= (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_instruction_infos)) {
        return 0;
    }
    return &asmkit_aarch64_instruction_infos[index];
}

const asmkit_instruction_info_t* asmkit_gen_aarch64_instruction_info(uint32_t id)
{
    size_t i;
    for (i = 0; i < ASMKIT_ARRAY_COUNT(asmkit_aarch64_instruction_infos); ++i) {
        if (asmkit_aarch64_instruction_infos[i].id == id) {
            return &asmkit_aarch64_instruction_infos[i];
        }
    }
    return 0;
}

uint32_t asmkit_gen_aarch64_instruction_count(void)
{
    return (uint32_t)ASMKIT_ARRAY_COUNT(asmkit_aarch64_instruction_infos);
}

const asmkit_operand_info_t* asmkit_gen_aarch64_instruction_operand_info(uint32_t id, uint32_t operand_index)
{
    const asmkit_instruction_info_t* info;
    info = asmkit_gen_aarch64_instruction_info(id);
    if (info == 0 || operand_index >= info->operand_info_count) {
        return 0;
    }
    return &asmkit_aarch64_operand_infos[info->operand_info_index + operand_index];
}

const asmkit_instruction_register_effect_t* asmkit_gen_aarch64_instruction_register_effect(uint32_t id, uint32_t effect_index)
{
    const asmkit_instruction_info_t* info;
    info = asmkit_gen_aarch64_instruction_info(id);
    if (info == 0 || effect_index >= info->register_effect_count) {
        return 0;
    }
    return &asmkit_aarch64_register_effect_infos[info->register_effect_index + effect_index];
}

#undef ASMKIT_GENERATED_OPTIONAL_TEXT_LITERAL
